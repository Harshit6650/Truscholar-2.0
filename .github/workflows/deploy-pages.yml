name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  # Lets you re-run a deploy from the Actions tab without a new commit.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; a newer push supersedes an in-flight run.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install
        run: npm ci

      - name: Build static export
        env:
          # Switches next.config.ts to output: 'export'.
          STATIC_EXPORT: '1'
          # A project page is served from /<repo>. basePath and assetPrefix
          # must match, or every asset 404s.
          PAGES_BASE_PATH: /Truscholar-2.0
          NEXT_PUBLIC_SITE_URL: https://harshit6650.github.io/Truscholar-2.0
        run: npm run build

      # Without this, GitHub's Jekyll step discards any directory starting with
      # an underscore — which is the entire _next asset folder.
      - name: Disable Jekyll
        run: touch out/.nojekyll

      # `enablement: true` switches Pages to the GitHub Actions source via the
      # API, so this does not depend on someone flipping it in Settings first.
      # Without it, this step fails on a repo still set to "Deploy from a
      # branch" — which is what happened on the first run, and why the legacy
      # Jekyll builder was serving the README instead of the site.
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
